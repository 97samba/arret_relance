import { Typography, TextField } from "@material-ui/core";

const Automator = () => {
    return (
        <div>
            <Typography> Envoyez-moi un commentaire</Typography>
            <TextField placeholder="Commentaire"></TextField>
        </div>
    );
};

export default Automator;
